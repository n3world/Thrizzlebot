module.exports = {
  channels: {"#npnd-dev" :
    {welcome: {salutation: "Welcome to #npnd-dev %s"}} // Provide channel specific configuration
  },
  server: "irc.freenode.net",
  botName: "ThrizzleBot-Dev",
  plugins: {
    haltOnLoadFailure: false,
    /* dir: "./plugins", // Optional, only if you want to move it */
    welcome: {
      salutation: "%s WHATUUUUUP",
      modes: ["o", "v"]
    }
    seen: {"key1":"value1"},
    badPlugin: false // This disables a plugin
  }
};
